{% extends '::base.html.twig' %}

{% block body %}

    <p>Want to contact symblog?</p>
    <section id="wrapper">
        <header id="header">
            <div class="top">
                {% block navigation %}
                    <nav>
                        <ul class="navigation">
                         <li><a href="{{path('BlogerBlogBundle_guests')}}">guests</a></li>
                          <li><a href="{{path('BlogerBlogBundle_contact')}}">comtact</a></li>
                        </ul>
                    </nav>
                {% endblock %}
            </div>


        </header>

   <form action="{{ path('acme_hello_homepage') }}" method="post" {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }} class="blogger">
        {{ form_errors(form) }}

        {{ form_row(form.name) }}
        {{ form_row(form.email) }}
        {{ form_row(form.subject) }}
        {{ form_row(form.body) }}

        {{ form_rest(form) }}

        <input type="submit" value="Submit" />
    </form>

        <aside class="sidebar">
            {% block sidebar %}{% endblock %}
        </aside>


    </section>



{% endblock %}